# AI-DIY Application Configuration
# Copy this file to .env and customize for your environment

# =============================================================================
# REQUIRED CONFIGURATION (Must be set for application to start)
# =============================================================================

# Logging level - Controls console output verbosity (file logs always capture DEBUG)
# Must be one of: DEBUG, INFO, WARNING, ERROR
# 
# DEBUG   - All messages including detailed execution traces (verbose console, large app.jsonl)
# INFO    - Normal operation messages (recommended for development)
# WARNING - Only warnings and errors (recommended for production)
# ERROR   - Only error messages (minimal logging)
#
# Note: 
# - Console output respects this level (prevents cluttering terminal in production)
# - File logs (src/logs/app.jsonl) always capture DEBUG for post-mortem analysis
# - Execution logs (execution_log_SP-*.jsonl) always include full test output
LOG_LEVEL=INFO

# Root directory for application data (visions, backlogs, wireframes)
DATA_ROOT=static/appdocs

# =============================================================================
# OPTIONAL CONFIGURATION (Has sensible defaults)
# =============================================================================

# Server configuration
PORT=8000
HOST=0.0.0.0

# Models configuration file path (relative to project root)
MODELS_CONFIG_PATH=models_config.json

# =============================================================================
# PRODUCTION-SPECIFIC CONFIGURATION
# =============================================================================

# Set to true for production deployment
PRODUCTION=false

# Production-specific settings (uncomment and configure as needed)
# OPENROUTER_API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here
# REDIS_URL=your_redis_url_here

# =============================================================================
# DEVELOPMENT-SPECIFIC CONFIGURATION
# =============================================================================

# Development settings (uncomment for development)
# DEBUG=true
# HOT_RELOAD=true