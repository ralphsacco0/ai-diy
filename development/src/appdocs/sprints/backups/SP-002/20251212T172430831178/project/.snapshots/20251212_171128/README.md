# BrightHR Lite

## Setup Instructions

1. Install Node.js v18+ via Homebrew: `brew install node`

2. Clone the repo: `git clone <repo-url>`

3. Install dependencies: `npm install`

4. Start the server: `npm start`. Access the app at http://localhost:3000

5. Login with admin@test.com / Password123!