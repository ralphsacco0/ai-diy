[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Persistent volume for data storage (includes both appdocs and execution-sandbox)
[[volumes]]
mountPath = "/app/development/src/static/appdocs"
